apiVersion: v1
kind: Service
metadata:
  name: inference-router
  namespace: default
spec:
  selector:
    ray.io/node-type: head
  ports:
  - name: http
    port: 80
    targetPort: 8000
  - name: prometheus
    port: 9090
    targetPort: 9090
